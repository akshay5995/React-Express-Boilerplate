import { connect } from 'react-redux';
import View from '../components/View';

// const mapStateToProps = (state) => {
//   return {

//   };
// };

// const mapDispatchToProps = (dispatch) => {
//   return {

//   };
// };

const ViewContainer = connect()(View);

export default ViewContainer;
